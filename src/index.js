import React from 'react';
import ReactDOM from 'react-dom'; //Mostly for render, which inserts this file into the core HTML file
import App from './App';

ReactDOM.render(<App />, document.getElementById('root'));
